{
  "name": "linrz",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vuepress dev src",
    "clean": "rm -rf assets category tag",
    "build": "npm run clean && vuepress build src && cp -rf dist/* . && rm -rf dist"
  },
  "dependencies": {
    "vuepress": "^1.2.0",
    "vuepress-theme-cute": "1.0.0"
  }
}
