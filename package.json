{
  "name": "linrz",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vuepress dev src",
    "clean": "rm -rf assets",
    "build": "npm run clean && vuepress build src && cp -rf dist/* . && rm -rf dist"
  },
  "dependencies": {
    "vuepress": "1.0.0-alpha.32",
    "vuepress-theme-cute": "2.0.5"
  },
  "resolutions": {
    "@vuepress/core": "1.0.0-alpha.32",
    "@vuepress/plugin-blog": "1.0.0-alpha.30",
    "@vuepress/plugin-pagination": "1.0.0-alpha.30"
  }
}
